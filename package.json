{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start -p 3000"
    
    npm install
    git add package-lock.json
git commit -m "Add package-lock.json"
git push


  }
}
